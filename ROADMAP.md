# ROADMAP — ICQ Messenger

Дорожная карта проекта: что сделано и что в очереди. Один пункт ≈ один ПР
(feature-ветка от `develop`). Порядок сверху вниз = приоритет.

## ✅ Сделано

| ПР | Фича |
|----|------|
| ПР2  | GUI на FXML + протокольные модели |
| ПР3  | Клиент-серверная сеть на сокетах |
| ПР4  | XML-протокол (JAXP/DOM) |
| ПР5  | Индикатор «печатает…» + статусы ONLINE/AWAY |
| ПР6  | Личные сообщения (выбор контакта в GUI) |
| ПР7  | PING keep-alive — детект мёртвых соединений |
| ПР8  | Двунаправленный PING/PONG |
| ПР9  | Персистентность переписки — журнал + история при входе |
| ПР10 | Персистентный список контактов (оффлайн-собеседники не пропадают) |
| ПР11 | Ротация журнала переписки по размеру |
| ПР12 | Аутентификация — регистрация + вход по паролю (PBKDF2), отдельный REGISTER |
| ПР13 | Оффлайн-доставка по-человечески — очередь недоставленного (`OfflineStore`), стабильный id сообщения, непрочитанное при входе вместо «истории», без ложного ERROR (честный ERROR только для несуществующего ника) |
| ПР14 | Галочки «прочитано» — типы `DELIVERED`/`READ`, значки ⏳/✓/✓✓ на своих личных пузырях; статус переживает перелогин (`ReadReceiptStore` + derive из очереди). Только личка |
| ПР15 | Передача файлов/картинок — чанковая загрузка на сервер (`FileStore`, лимит 10 МБ), сообщение-ссылка через обычный `MESSAGE` (оффлайн/история/галочки даром), скачивание по запросу, картинки инлайн + файлы карточкой, сохранение через диалог |
| ПР16 | Групповые чаты — именованные комнаты `#имя` в поле `to` (`RoomStore`, персист `data/rooms.log`). Войти/создать по имени, покинуть (контекст-меню), список комнат + участники в заголовке. Доставка онлайн-участникам + оффлайн-очередь, история и файлы в комнатах — даром из ПР13/9/15. Галочек в комнатах нет (как и в общем чате) |
| ПР17 | Реконнект клиента — при обрыве (нет PONG / разрыв чтения) `ClientService` не сдаётся, а крутит фоновый цикл переподключения с backoff (переживает окно реапера «уже в сети» ~15с), затем перезапускает reader+heartbeat. Дедуп по `id` (`seenMessageIds`) гасит дубли повторно проигранной истории; исходящим бродкаст/комнатам тоже выдаётся `id`. События `onReconnecting`/`onReconnected`, статус в заголовке, гард отправки без связи |

## 🔜 В очереди

| № | Фича | Заметки |
|---|------|---------|
| 1 | **Шифрование канала** | TLS на сокете — сейчас всё идёт открытым текстом (в т.ч. пароль при входе). **← следующий (ПР18)** |
| 2 | **Валидация ника + анти-спам** | Лимит/проверка ника при логине, rate-limit сообщений. |
| 3 | **Уведомления** | Звук/мигание окна на входящее, когда окно не в фокусе. |
| 4 | **Поиск по истории** | Поиск по ленте + группировка сообщений по датам. |

## Правила процесса
- Поток веток: `main` / `develop` / `feature/*`. В `main` напрямую не коммитим.
- Новую `feature/prXX-*` режем **от `develop` ДО** начала правок.
- Сборка под JDK 26.
